# Database connection
spring.datasource.url=jdbc:mysql://localhost:3306/petstore
spring.datasource.username=tinapay
spring.datasource.password=tinapay18
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA config
spring.jpa.hibernate.ddl-auto=update

# Optional: Show SQL in the console (for debugging)
spring.jpa.show-sql=true

# Optional: Automatically create tables from @Entity classes
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect